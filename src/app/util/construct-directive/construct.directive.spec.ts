import { ConstructDirective } from './construct.directive';

describe('ConstructDirective', () => {
  it('should create an instance', () => {
    const directive = new ConstructDirective();
    expect(directive).toBeTruthy();
  });
});
